ext jpg = feh --scale-down --auto-zoom -d "$@" --auto-rotate --draw-actions
ext png = feh --scale-down --auto-zoom -d "$@"
ext pdf = apvlv "$@"
ext (tsx?|jsx?|s?css|html|md|json|php) = nvim "$@"
ext (mov|mp4|avi|asf) = vlc "$@"

